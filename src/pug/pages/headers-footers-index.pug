extends ../layout/headers-footers-main

block variables
  - var title = 'Headers and footers';
  - var description = 'Description for Webpack + Pug';
  - var pageClass = 'index-page';

//- block content
  section
    .container
      +item('item title', 'item descr')
      +item('item title', 'item descr')
      +item('item title', 'item descr')
  example-component


block script
  //- script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }